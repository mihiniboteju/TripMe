# Test Environment Configuration
NODE_ENV=test

# Server Configuration
PORT=5555

# JWT Configuration
JWT_SECRET=test-secret-key-for-testing-only-do-not-use-in-production

# MongoDB Configuration (uses in-memory MongoDB)
# MONGODB_URI is handled by mongodb-memory-server in tests
MONGODB_URI=mongodb://localhost:27017/tripme-test

# Email Configuration (mocked in tests)
EMAIL_USER=test@example.com
EMAIL_PASS=test-password
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587

# Cloudinary Configuration (mocked in tests)
CLOUDINARY_CLOUD_NAME=test-cloud
CLOUDINARY_API_KEY=test-api-key
CLOUDINARY_API_SECRET=test-api-secret

# Test-specific settings
JEST_TIMEOUT=30000
