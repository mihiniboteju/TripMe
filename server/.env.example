# ⚠️ SECURITY WARNING: Copy this file to .env and configure with YOUR credentials
# ⚠️ NEVER commit .env file to git - it contains sensitive information!
# ⚠️ The .env file is already in .gitignore

# Server Configuration
# Note: Container runs on port 5000 internally, mapped to 5001 externally (see docker-compose.yml)
PORT=5000

# MongoDB Configuration
MONGO_URI=mongodb://mongodb:27017/mernAuth

# JWT Configuration
# ⚠️ CRITICAL: Generate a secure random JWT_SECRET for production!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_change_in_production

# Email Configuration (for OTP)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password_here

# Cloudinary Configuration (if using)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
