# ⚠️ Copy this file to .env before running
# ⚠️ NEVER commit .env file to git!

# API Base URL - BlogApp3 Backend endpoint
# React/Vite apps run in the BROWSER, so they need localhost to access the backend
# The backend container exposes port 5002 to the host machine
VITE_API_BASE_URL=http://localhost:5002
